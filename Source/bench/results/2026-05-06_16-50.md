# Token Comparison Report

Generated: 2026-05-06 16:51  
Model: `claude-haiku-4-5-20251001`  
Questions: 5

---

## Summary

| Approach | S1 input | S2 input | S2 output | Total | vs full code |
|---|---:|---:|---:|---:|---:|
| keyword   | 0 | 4,526 | 1,133 | **5,659** | 21.7x cheaper |
| llm-sel   | 7,219 | 8,980 | 1,421 | **17,832** | 6.9x cheaper |
| full code | 0 | 120,169 | 2,797 | **122,966** | baseline |

---

## Q1: what does cmd_extract do?

| Approach | S1 input | S2 input | S2 output | Total | Nodes |
|---|---:|---:|---:|---:|---|
| keyword   | 0 | 194 | 133 | **327** | cmd_extract |
| llm-sel   | 1,443 | 468 | 111 | **2,047** | extract, file_scanner, parser, builder, merger |
| full code | 0 | 24,033 | 414 | **24,447** | - |

vs full code: keyword **74.8x** cheaper · llm-sel **11.9x** cheaper

**keyword:**

`cmd_extract` in `cli.py` does the following:

1. **Merges a project** - It calls `merge_project()` from `extraction.merger` with the input path, output path, and project name from the provided arguments.

2. **Optionally loads to Neo4j** - If the `args.load_neo4j` flag is set, it imports and calls `load()` from `graph.loader` to load the merged project data into a Neo4j database.

It's a CLI command handler that orchestrates project extraction and optional graph database loading.

**llm-select:**

I don't see `cmd_extract` in the provided graph context. The context shows an `extract` function in `C:/Users/Admin/OneDrive/Tài liệu/PA/extraction/extractor.py` that takes an AST tree, file path, and source code, then returns a tuple of graph nodes and edges.

Could you clarify if you meant the `extract` function, or provide more context about where `cmd_extract` is located in the codebase?

**full code:**

# What does `cmd_extract` do?

`cmd_extract` is a CLI command handler in **cli.py** that extracts a Python codebase and converts it into a structured graph representation.

## Functionality

```python
def cmd_extract(args):
    from extraction.merger import merge_project
    merge_project(args.input, args.output, args.name)
    if args.load_neo4j:
        from graph.loader import load
        load(args.output)
```

**Step by step:**

1. **`merge_project(args.input, args.output, args.name)`** — the main extraction pipeline:
   - Scans all `.py` files in the input directory (excluding `__init__.py`)
   - Parses each file and extracts nodes (modules, classes, functions) and edges (imports, calls, inheritance, etc.)
   - Deduplicates nodes by ID
   - Resolves cross-file references (imports, function calls, class inheritance)
   - Writes the result to a JSON graph file at `args.output`

2. **`load(args.output)`** (optional) — if `--load-neo4j` flag is set:
   - Loads the generated JSON graph into a Neo4j database for graph querying

## Usage

```bash
caeser extract --input . --output data/graph.json --name my_project [--load-neo4j]
```

The output JSON contains:
- **Nodes**: modules, classes, functions with metadata (docstrings, source code, line numbers)
- **Edges**: relationships between nodes (confidence scores indicate whether they're internal or external)

This graph can then be queried with `caeser query` or exported to Obsidian with `caeser export`.

---

## Q2: how does the extractor parse functions?

| Approach | S1 input | S2 input | S2 output | Total | Nodes |
|---|---:|---:|---:|---:|---|
| keyword   | 0 | 159 | 254 | **413** | extractor |
| llm-sel   | 1,444 | 2,046 | 363 | **3,909** | _handle_function, visit_FunctionDef, visit_AsyncFunctionDef, _collect_calls, _decorator_name, _resolve_name, visit_ClassDef, extract |
| full code | 0 | 24,034 | 764 | **24,798** | - |

vs full code: keyword **60.0x** cheaper · llm-sel **6.3x** cheaper

**keyword:**

# Function Parsing in Extractor

Based on the graph context, the `extractor.py` module uses several key functions to parse functions:

1. **`extract`** - The main entry point that orchestrates function extraction

2. **`_Extractor`** - A class (likely using AST traversal) that handles the core parsing logic

3. **`_resolve_name`** - Resolves function/variable names, probably to handle aliasing and imports

4. **`_decorator_name`** - Extracts decorator information from functions

5. **`_collect_calls`** - Collects function calls within the parsed code

The module appears to use **Abstract Syntax Tree (AST) analysis** to parse Python functions and extract their structure, including decorators, names, and call dependencies.

However, I'd need to examine the actual implementation in `C:/Users/Admin/OneDrive/Tài liệu/PA/extraction/extractor.py` to provide specific details about parsing logic, data structures, and how these functions interact. Could you share the implementation code or specific parsing behavior you're interested in?

**llm-select:**

# Function Parsing in the Extractor

The extractor parses functions through two visitor methods in `_Extractor`:

## Visitor Methods

**`visit_FunctionDef`** (line 93) and **`visit_AsyncFunctionDef`** (line 96) both delegate to **`_handle_function`** (line 99), which:

## Key Steps in `_handle_function`

1. **Creates a unique ID** based on context:
   - If inside a class: `func:{file_path}::{class_name}::{func_name}` (method)
   - If top-level: `func:{file_path}::{func_name}`

2. **Creates a GraphNode** with:
   - Name, type (`NodeType.FUNCTION`)
   - Metadata: line number, docstring, source code, async flag
   - Decorators (extracted via `_decorator_name`)

3. **Creates containment/definition edges**:
   - Methods: `CONTAINS` edge to parent class
   - Top-level functions: `DEFINES` edge to module

4. **Collects function calls** via `_collect_calls`:
   - Uses an AST visitor to find all `Call` nodes
   - Stops at nested functions (doesn't recurse into them)
   - Each call creates a `CALLS` edge with a "ghost ID" (`func_ref:{call_name}`) for later resolution

The design separates immediate graph construction from call resolution, allowing the resolver to map ghost IDs to actual function nodes later.

**full code:**

# How the Extractor Parses Functions

The extractor parses functions in **extraction/extractor.py** through the `_Extractor` class, which is an AST visitor. Here's how it works:

## Main Entry Point
The `extract()` function receives an AST and file path, creates an `_Extractor` visitor, and returns nodes and edges:

```python
def extract(tree: ast.AST, file_path: str, source: str = "") -> Tuple[List[GraphNode], List[GraphEdge]]:
    visitor = _Extractor(file_path, source)
    visitor.visit(tree)
    return visitor.nodes, visitor.edges
```

## Function Parsing Process

### 1. **Visitor Methods**
The extractor handles both regular and async functions:
- `visit_FunctionDef()` → calls `_handle_function(node, is_async=False)`
- `visit_AsyncFunctionDef()` → calls `_handle_function(node, is_async=True)`

### 2. **Node Creation** 
For each function, it creates a `GraphNode` with:
- **ID scheme**: Differs based on context:
  - **Top-level function**: `func:path/to/file.py::function_name`
  - **Method (inside class)**: `func:path/to/file.py::ClassName::method_name`
- **Metadata**: name, type, file path, line number, docstring, source code, async flag, decorators

### 3. **Edge Creation**
Two types of edges are created:

**Container edge** (connects function to its parent):
- Methods: `CONTAINS` edge from class to method
- Top-level: `DEFINES` edge from module to function (confidence=1.0)

**Call edges** (identifies what the function calls):
- Uses `_collect_calls()` helper to scan function body
- Creates `CALLS` edges with `func_ref:` ghost IDs (confidence=0.0)
- Ghost IDs are resolved later by `ImportResolver`

### 4. **Call Collection**
The `_collect_calls()` helper:
- Walks the function body AST looking for `Call` nodes
- Extracts called function names using `_resolve_name()` (handles simple names, attributes like `obj.method()`)
- **Stops at nested functions** — doesn't recurse into inner function definitions to avoid noise

### 5. **Context Tracking**
The extractor tracks the current class context:
```python
self._current_class_id      # ID of enclosing class (if any)
self._current_class_name    # Name of enclosing class
```
This allows it to distinguish methods (`CONTAINS`) from module-level functions (`DEFINES`).

## Example
For this code:
```python
class User:
    def save(self):
        db.commit()
```

The extractor creates:
- Node: `class:file.py::User`
- Node: `func:file.py::User::save`
- Edge: `CONTAINS` from class to method
- Edge: `CALLS` from method to `func_ref:db.commit` (unresolved, confidence=0.0)

---

## Q3: what is the role of ImportResolver?

| Approach | S1 input | S2 input | S2 output | Total | Nodes |
|---|---:|---:|---:|---:|---|
| keyword   | 0 | 752 | 335 | **1,087** | ImportResolver |
| llm-sel   | 1,444 | 1,732 | 356 | **3,577** | resolve, _resolve_one, _resolve_module, _resolve_class, _resolve_func, _build_maps, _to_dotted, _make |
| full code | 0 | 24,034 | 390 | **24,424** | - |

vs full code: keyword **22.5x** cheaper · llm-sel **6.8x** cheaper

**keyword:**

# ImportResolver Role

The `ImportResolver` class in `C:/Users/Admin/OneDrive/Tài liệu/PA/extraction/resolver.py` serves as a **ghost ID resolution engine**. Its primary roles are:

1. **Resolve Raw Edges**: Takes a list of nodes and edges containing "ghost IDs" (unresolved references) and maps them to actual node IDs in the graph.

2. **Handle Three Types of Ghost IDs**:
   - `dep:module.name` → maps to `module:path`
   - `class_ref:ClassName` → maps to `class:path::Name`
   - `func_ref:func_name` → maps to `func:path::Name` (best-effort)

3. **Build Resolution Maps**: Constructs three internal maps during initialization:
   - `_module_map`: dotted module names to node IDs
   - `_class_map`: class names to node IDs (list, since names can be non-unique)
   - `_func_map`: function names to node IDs (list, same reason)

4. **Handle Unresolvable References**: For edges that cannot be resolved (external libraries, builtins), it sets `confidence=0.0` so they can be filtered out later by the export/query layer.

In essence, it's the **symbol resolution layer** that bridges raw extraction output (with incomplete references) to a fully resolved graph structure.

**llm-select:**

# ImportResolver Role

`ImportResolver` (in `C:/Users/Admin/OneDrive/Tài liệu/PA/extraction/resolver.py`) is responsible for **resolving symbolic references in the code dependency graph** into actual node IDs.

## Key Functions:

1. **Builds resolution maps** (`_build_maps`):
   - Maps module dotted names → module node IDs
   - Maps class names → class node IDs
   - Maps function names → function node IDs

2. **Resolves graph edges** (`resolve`):
   - Processes edges with symbolic references (prefixed with `dep:`, `class_ref:`, `func_ref:`)
   - Converts them to concrete node IDs

3. **Handles three types of references** (`_resolve_one`):
   - **`dep:`** → resolves to actual module using `_resolve_module`
   - **`class_ref:`** → resolves to actual class using `_resolve_class`
   - **`func_ref:`** → resolves to actual function using `_resolve_func` (with confidence scoring)

4. **Confidence scoring** (`_resolve_func`):
   - Returns 0.7 for unambiguous matches (single function with that name)
   - Returns 0.5 for ambiguous matches, preferring functions in the same file as caller
   - Returns 0.0 for unresolved references

In summary: **ImportResolver transforms abstract symbol references into concrete dependency graph connections**, enabling accurate code dependency analysis.

**full code:**

# ImportResolver Role

`ImportResolver` (in `extraction/resolver.py`) is responsible for **resolving ghost IDs to actual node IDs** in the codebase graph.

## Key Responsibilities

1. **Resolves Three Types of Ghost IDs:**
   - `dep:module.name` → maps to actual `module:path` node IDs
   - `class_ref:ClassName` → maps to actual `class:path::Name` node IDs
   - `func_ref:func_name` → maps to actual `func:path::Name` node IDs (best-effort)

2. **Builds Internal Lookup Maps:**
   - `_module_map`: dotted module names to node IDs (e.g., `"extraction.extractor"` → `module:extraction/extractor.py`)
   - `_class_map`: class names to their node IDs
   - `_func_map`: function names to their node IDs

3. **Sets Confidence Scores:**
   - Resolved edges: `confidence = 1.0` (or 0.7/0.5 for ambiguous function calls)
   - Unresolved edges (external libs, ambiguous): `confidence = 0.0` (filtered out later)

## Workflow

The extractor initially creates edges with **ghost IDs** because it doesn't know if references are internal project code or external libraries. The `ImportResolver` then:

```python
resolver = ImportResolver(str(root), deduped_nodes)
resolved_edges = resolver.resolve(all_edges)
```

This converts unresolved edges into concrete node-to-node relationships, enabling accurate graph queries and exports.

---

## Q4: how does QueryEngine find relevant nodes?

| Approach | S1 input | S2 input | S2 output | Total | Nodes |
|---|---:|---:|---:|---:|---|
| keyword   | 0 | 617 | 240 | **857** | QueryEngine |
| llm-sel   | 1,444 | 2,522 | 242 | **4,251** | QueryEngine, find_nodes, _node_summary, build_context, GraphNode, NodeType, extract, merge_project |
| full code | 0 | 24,034 | 521 | **24,555** | - |

vs full code: keyword **28.7x** cheaper · llm-sel **5.8x** cheaper

**keyword:**

# QueryEngine Node Finding Strategy

The `QueryEngine.find_nodes()` method in `C:/Users/Admin/OneDrive/Tài liệu/PA/query/engine.py` finds relevant nodes through a two-pass approach:

## Pass 1: Exact Name Matching
- Converts the question to lowercase
- Searches for nodes where the node's name appears as a substring in the question
- Tracks seen nodes to avoid duplicates

## Pass 2: Partial/Word Matching
- If no exact matches found, performs a secondary search
- Splits the question into words (length > 3 characters)
- Finds nodes where either:
  - The node name contains any question word, OR
  - Any question word contains the node name

## Result
- Returns up to 8 matched nodes (`matched[:8]`)
- The method prioritizes exact matches first, falling back to fuzzy word-level matching if needed

This approach balances precision (exact matching) with recall (partial word matching) to handle various question phrasings.

**llm-select:**

The `QueryEngine.find_nodes()` method in `C:/Users/Admin/OneDrive/Tài liệu/PA/query/engine.py:26` finds relevant nodes using a two-stage matching strategy:

**Stage 1: Exact substring match**
- Converts the question to lowercase
- Searches for nodes whose name is a substring of the question
- Stops here if any matches are found

**Stage 2: Fuzzy word matching** (fallback if Stage 1 finds nothing)
- Splits the question into words (length > 3 characters)
- For each node, checks if:
  - The node name appears in any word, OR
  - Any word appears in the node name
- This catches partial matches and variations

**Result**
- Returns up to 8 matched nodes (`matched[:8]`)
- Uses a `seen` set to avoid duplicates

This approach prioritizes exact matches but falls back to fuzzy matching when needed, making it flexible for finding classes, functions, or modules related to the user's question.

**full code:**

# How QueryEngine Finds Relevant Nodes

The `QueryEngine` in `query/engine.py` uses a **two-stage LLM approach** with a keyword fallback:

## Stage 1: LLM Node Selection (`_select_nodes_llm`)

1. **Builds a node catalog** via `_build_node_catalog()`:
   - Creates a searchable index: `name | type | file:line`
   - Example: `merge_project | Function | merger.py:10`

2. **Calls Claude Haiku** to pick relevant nodes:
   - Sends the catalog + question to the LLM
   - Asks it to return a JSON array of up to 8 matching node names
   - **Constraint**: Only returns names that exist in the catalog (prevents hallucination)

3. **Returns matching nodes**:
   - Uses `_name_to_node` lookup to convert selected names back to full node objects
   - Returns both the nodes and token usage data

## Stage 2: Keyword Fallback (`_find_nodes_keyword`)

If LLM selection fails or returns nothing, it falls back to **word-boundary matching**:

1. **Extracts query words** (lowercase, 3+ chars):
   - Example: "how does QueryEngine find nodes?" → {how, does, queryengine, find, nodes}

2. **First pass** - exact name match:
   - Checks if any node name exactly matches a query word

3. **Second pass** - fuzzy word matching (if no first-pass hits):
   - Splits CamelCase node names into word parts
   - Matches against 5+ char words from the question
   - Example: `FindNodesByName` → {find, nodes, by, name}

## Full Flow (`find_nodes`)

```python
def find_nodes(self, question: str) -> list[dict]:
    nodes, _ = self._select_nodes_llm(question)
    return nodes or self._find_nodes_keyword(question)  # Try LLM first, fallback to keyword
```

**Key advantage**: The LLM stage is semantic (understands intent), while the keyword stage ensures graceful degradation when LLM fails.

---

## Q5: how is the graph exported to JSON?

| Approach | S1 input | S2 input | S2 output | Total | Nodes |
|---|---:|---:|---:|---:|---|
| keyword   | 0 | 2,804 | 171 | **2,975** | graph_viewer, build_file_level_graph, build_file_detail_graph, GraphViewerApp, _build_full_graph, launch_graph_viewer, GraphNode, GraphEdge |
| llm-sel   | 1,444 | 2,212 | 349 | **4,048** | build_graph, merge_project, _node_dict, _edge_dict, GraphNode, GraphEdge, export, loader |
| full code | 0 | 24,034 | 708 | **24,742** | - |

vs full code: keyword **8.3x** cheaper · llm-sel **6.1x** cheaper

**keyword:**

I don't have information about JSON export in the provided graph context. The context shows:

- **graph_viewer.py** — Contains graph viewing and visualization logic (uses networkx MultiDiGraph internally)
- **extraction/models.py** — Defines GraphNode and GraphEdge data classes

However, the context doesn't include any export functions or JSON serialization code. To find the JSON export functionality, I would need to see:

1. Additional modules that might handle export (e.g., `export.py`, `serializer.py`)
2. Functions in `builder.py` or other files that convert the graph to JSON format

Could you check if there's an export or serialization module in the codebase, or provide more context about where JSON export happens?

**llm-select:**

# Graph Export to JSON

The graph is exported to JSON through the **`merge_project`** function in `C:/Users/Admin/OneDrive/Tài liệu/PA/extraction/merger.py`.

## Process:

1. **Collects nodes and edges** from all Python files (excluding `__init__.py`)
2. **Deduplicates nodes** by ID to avoid duplicates across files
3. **Resolves cross-file references** using the `resolve()` function
4. **Converts to dictionaries** using helper functions:
   - `_node_dict()` — converts `GraphNode` objects to dicts with fields: `id`, `name`, `type`, `file_path`, `line_start`, `docstring`, `source_code`, `is_async`, `decorators`
   - `_edge_dict()` — converts `GraphEdge` objects to dicts with fields: `type`, `source_id`, `target_id`, `confidence`

5. **Writes JSON** to disk containing:
   - `project` name
   - `nodes` list
   - `edges` list (only edges with `confidence > 0.0` are typically used later)

The JSON format is then consumed by:
- **Graph building** via `build_graph()` (creates NetworkX MultiDiGraph)
- **Export to Obsidian** via `export()` which filters edges by `confidence > 0.0` to avoid broken references to unresolved external libraries

**full code:**

# Graph Export to JSON

The graph is exported to JSON through the **`merge_project` function** in `extraction/merger.py`. Here's how it works:

## Main Export Flow

```python
def merge_project(project_root: str, output_path: str, project_name: str = None) -> dict:
```

### Steps:

1. **Scan Python files** — excludes `__init__.py` to prevent duplicate Module nodes
2. **Extract nodes & edges** — parses each file using `parse_file()` and `extract()`
3. **Dedup nodes** — keeps first occurrence of each node ID to avoid duplicates
4. **Resolve cross-file references** — uses `ImportResolver` to map ghost IDs to real node IDs
5. **Build JSON structure** — creates the final graph dictionary:

```python
graph = {
    "project": project_name,
    "project_root": str(root),
    "extracted_at": datetime.now().isoformat(),
    "file_count": len(files),
    "nodes": [_node_dict(n) for n in deduped],
    "edges": [_edge_dict(e) for e in resolved_edges],
}
```

6. **Write to disk** — serializes to JSON file with UTF-8 encoding:

```python
out.write_text(json.dumps(graph, indent=2, ensure_ascii=False), encoding="utf-8")
```

## Node Serialization

The `_node_dict()` function converts `GraphNode` objects to JSON-friendly dicts:

```python
def _node_dict(n: GraphNode) -> dict:
    return {
        "id": n.id,
        "name": n.name,
        "type": n.type.value,  # "Module", "Class", or "Function"
        "file_path": n.file_path,
        "line_start": n.line_start,
        "docstring": n.docstring,
        "source_code": n.source_code,
        "is_async": n.is_async,
        "decorators": n.decorators,
    }
```

## Edge Serialization

The `_edge_dict()` function converts edges, **filtering out unresolved ones** (confidence = 0.0):

```python
def _edge_dict(e: GraphEdge) -> dict:
    return {
        "type": e.type.value,        # "IMPORTS", "DEFINES", etc.
        "source_id": e.source_id,
        "target_id": e.target_id,
        "confidence": e.confidence,   # 1.0 = resolved, 0.0 = external/ambiguous
    }
```

## Usage

Called via CLI:
```bash
python cli.py extract --input /path/to/project --output data/graph.json
```

Or directly from `app/main.py` in the viewer pipeline.

---
